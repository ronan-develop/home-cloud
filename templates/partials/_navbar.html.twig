<nav class="bg-gray-900 shadow-md">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-between h-16">
      <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
        <div class="flex-shrink-0 flex items-center">
          <img class="block lg:hidden h-8 w-auto rounded-md" src="{{ asset('img/logo.png') }}" alt="logo">
          <img class="hidden lg:block h-8 w-auto rounded-md" src="{{ asset('img/logo.png') }}" alt="logo">
        </div>
        <div class="hidden sm:block sm:ml-6">
          <div class="flex space-x-4">
            <a href="{{ path('app_home') }}" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Accueil</a>
            {% if app.user %}
              <a href="{{ path('app_user_edit', {'id': app.user.id}) }}" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Mon profil</a>
            {% endif %}
          </div>
        </div>
      </div>
      {% if app.user %}
        <span class="text-gray-300 px-3 py-2 rounded-md text-sm font-medium float-right">{{ app.user.userIdentifier }}</span>
        <a href="{{ path('app_logout') }}" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Déconnexion</a>
      {% endif %}
      
    </div>
  </div>
</nav>