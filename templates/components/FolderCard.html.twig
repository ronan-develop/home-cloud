{# Composant FolderCard — Carte dossier/fichier #}
<div class="folder-card">
  <div class="folder-icon">{{ item.isFolder ? '📁' : '📄' }}</div>
  <div class="folder-name">{{ item.name }}</div>
  {# Ajout d’actions ou d’infos complémentaires possible ici #}
</div>
{# Props attendues : item (objet dossier/fichier) #}
