###> Variables d'environnement — Production HomeCloud
###> Ce fichier est un template. Copier en .env.local et remplir les valeurs.
###> NE JAMAIS commiter .env.local

APP_ENV=prod
APP_DEBUG=0

# Générer avec : php -r "echo bin2hex(random_bytes(16));"
APP_SECRET=CHANGE_ME

# Format MySQL o2switch : mysql://user:password@127.0.0.1:3306/dbname?serverVersion=mariadb-10.6.0&charset=utf8mb4
DATABASE_URL=mysql://ron2cuba_PRENOM:DB_PASSWORD@127.0.0.1:3306/ron2cuba_PRENOM?serverVersion=mariadb-10.6.0&charset=utf8mb4

# URL publique de cette instance
APP_URL=https://PRENOM.lenouvel.me

# CORS — autoriser le front à appeler cette API
CORS_ALLOW_ORIGIN=^https://PRENOM\.lenouvel\.me$

# Chiffrement au repos — générer avec :
# php -r "echo base64_encode(sodium_crypto_secretstream_xchacha20poly1305_keygen());"
# ⚠️ NE JAMAIS CHANGER après le premier upload (fichiers irrécupérables)
APP_ENCRYPTION_KEY=CHANGE_ME

# Messenger — transport base de données (pas de RabbitMQ sur o2switch)
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0

# JWT — chemins vers les clés (générées par le script de déploiement)
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=CHANGE_ME
JWT_TTL=3600
