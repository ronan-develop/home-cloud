framework:
    rate_limiter:
        share_creation:
            policy: 'sliding_window'
            limit: 20
            interval: '15 minutes'
        reset_password_request:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        reset_password_submit:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
