# Fichier d'exemple — copier vers .env.local et remplir les valeurs réelles
# .env.local est dans .gitignore : ne jamais commiter les secrets

APP_ENV=prod
# OBLIGATOIRE — générer avec : openssl rand -hex 32
APP_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32

# OBLIGATOIRE — clé de chiffrement des fichiers (sodium secretstream XChaCha20-Poly1305)
# Générer avec : php -r "echo base64_encode(sodium_crypto_secretstream_xchacha20poly1305_keygen());"
APP_ENCRYPTION_KEY=CHANGE_ME_generate_with_php_sodium

DATABASE_URL="mysql://user:CHANGE_ME_password@127.0.0.1:3306/home_cloud?serverVersion=10.6.0-MariaDB&charset=utf8mb4"

# Origine(s) autorisée(s) pour CORS — restreindre au domaine de prod exact
CORS_ALLOW_ORIGIN='^https://yourdomain\.com$'

# Transport Messenger (doctrine = pas besoin de RabbitMQ)
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
